{
  "name": "octoprint-translatemodel-lint",
  "private": true,
  "description": "Dev-only tooling for linting the plugin's browser assets. Not published, not shipped in the wheel.",
  "devDependencies": {
    "@eslint/js": "^10.8.0",
    "eslint": "^10.8.0",
    "eslint-plugin-no-unsanitized": "^4.1.2"
  }
}
